# Default configuration file (used when no specific profile is active)
some.property=Default Configuration
environment=Default
welcome.message=Welcome to the Default Environment!
application.version=1.0.0
